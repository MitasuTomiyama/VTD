
<% @artists.each do |artist| %>

  <%= link_to artist_path(artist) do %>
    <%# <%= attachment_image_tag artist, :image, :fill, 100, 100, fallback: "no_image.png", size: '100x100'%>
    <%= image_tag artist.image_id.to_s, fallback: "no_image.png", size: '100x100' %>
  <% end %>

  <%= artist.name %>

<% end %>
